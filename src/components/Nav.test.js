import * as React from 'react'
import { fireEvent, render, screen } from '@testing-library/react'
import { GeolocationContext, GeolocationProvider } from '../context/GeolocationContext';
import Nav from './Nav';


test('test Nav', () => {
  // const comp = render(<Nav />)

  // comp.debug()
});